{
  "name": "grails",
  "version": "1.0.0",
  "description": "Grails — A New Art Marketplace",
  "private": true,
  "scripts": {
    "deploy": "vercel --prod",
    "build": "echo 'static site, no build needed'"
  },
  "devDependencies": {
    "@types/node": "^20.19.35",
    "vercel": "^37.0.0"
  }
}
